name: rimu-cloud
services:
  db:
    image: pgvector/pgvector:pg18-trixie
    container_name: postgres
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      #- postgres_data:/var/lib/postgresql/data
      - ./.docker-volumes/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 5
    # restart: always
  web-api:
    build:
      context: .
      dockerfile: src/RimuCloud.ApiService/Dockerfile
    container_name: web-api
    # ports:
    #   - "${WEB_API_PORT}:8080"
    environment:
      - TZ=${TZ}
      - DOTNET_HOSTBUILDER__RELOADCONFIGONCHANGE=false
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - DbConfig__Host=postgres
      - DbConfig__Port=5432
      - DbConfig__User=${POSTGRES_USER}
      - DbConfig__Password=${POSTGRES_PASSWORD}
      - DbConfig__DbName=${POSTGRES_DB}
    volumes:
      - /data/rimu-cloud/logs/web-api:/var/log
    depends_on:
      migration:
        condition: service_completed_successfully
      db:
        condition: service_healthy
    # restart: always
  migration:
    build:
      context: .
      dockerfile: src/RimuCloud.MigrationService/Dockerfile
    container_name: migration
    environment:
      - TZ=${TZ}
      - DOTNET_HOSTBUILDER__RELOADCONFIGONCHANGE=false
      - DOTNET_ENVIRONMENT=Development
      - DbConfig__Host=postgres
      - DbConfig__Port=5432
      - DbConfig__User=${POSTGRES_USER}
      - DbConfig__Password=${POSTGRES_PASSWORD}
      - DbConfig__DbName=${POSTGRES_DB}
    volumes:
      - /data/rimu-cloud/logs/migration:/var/log
    #restart: always
    depends_on:
      db:
        condition: service_healthy
  reverse-proxy:
    image: nginx:stable-alpine
    container_name: reverse-proxy
    ports:
      - 80:80
      - "${NGINX_LISTEN_PORT}:443"
    volumes:
      - ./conf/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./conf/nginx/nginx-selfsigned.crt:/etc/nginx/ssl/nginx-selfsigned.crt
      - ./conf/nginx/nginx-selfsigned.key:/etc/nginx/ssl/nginx-selfsigned.key
    depends_on:
      - web-api
# volumes:
#   postgres_data:
#   solr_data:
  
